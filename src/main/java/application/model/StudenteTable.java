package application.model;

// Modello per la tabella degli studenti con valutazioni

public record StudenteTable(String nome, String cognome, String dataValutazione, Integer voto, String username) {
}
